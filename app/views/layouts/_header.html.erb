<header >
  <div >
    <nav>
      <ul >
       <% if logged_in? %>
          <li>
          <%= link_to 'Create new posts', new_post_path, class: 'new_post' %>
          </li>
              <li>
                <%= link_to "Sign out", logout_path, method: :delete %>
              </li>
        <% else %>
          <li><%= link_to "Sign in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
